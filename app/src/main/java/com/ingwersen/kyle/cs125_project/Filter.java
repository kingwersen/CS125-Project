package com.ingwersen.kyle.cs125_project;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

/**
 * Created by kyle on 3/8/2018.
 */

public class Filter
{
    /*
    TODO: Update with DataListItem
    public void apply(String filter, ArrayList<StoreItem> items)
    {
        // Retrieve a list of all words the user requested.
        // TODO: Expand filter based on similar terms.
        List<String> terms = Arrays.asList(filter.split(" "));
        int len = terms.size();
        for (int i = 0; i < len; ++i) {
            terms.set(i, terms.get(i).toLowerCase());
        }

        // Produce a mask based on whether each object is
        len = items.size();
        for (int i = 0; i < len; ++i)
        {
            items.get(i).setVisible(terms.contains(items.get(i).toString()));
        }
    }
    */
}
